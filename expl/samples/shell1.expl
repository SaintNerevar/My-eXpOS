int main() {
    decl
        int temp;
        str filename;
    enddecl
    begin
        temp = exposcall("Write", -2, "Enter file");
        temp = exposcall("Read", -1, filename);

        temp = exposcall("Exec", filename);
        return 0;
    end
}